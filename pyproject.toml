[project]
name = "qwen-image"
version = "0.1.0"
description = "Local Qwen3.5 multimodal inference setup with vLLM and llama.cpp."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "hf-transfer>=0.1.9",
  "huggingface-hub>=1.6.0",
  "openai>=1.0.0",
]
